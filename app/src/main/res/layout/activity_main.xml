<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black"
    tools:context=".MainActivity">

    <!-- 1. PreviewView: Hiển thị luồng camera, chiếm toàn bộ màn hình -->
    <androidx.camera.view.PreviewView
        android:id="@+id/previewView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- 2. MRZ Guide Frame + scrims -->
    <!-- Left scrim -->
    <View
        android:id="@+id/scrim_left"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="#80000000"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/mrz_guide_frame"/>

    <!-- Top scrim -->
    <View
        android:id="@+id/scrim_top"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="#80000000"
        app:layout_constraintStart_toStartOf="@+id/mrz_guide_frame"
        app:layout_constraintEnd_toEndOf="@+id/mrz_guide_frame"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/mrz_guide_frame"/>

    <!-- Right scrim -->
    <View
        android:id="@+id/scrim_right"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="#80000000"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@+id/mrz_guide_frame"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Bottom scrim -->
    <View
        android:id="@+id/scrim_bottom"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="#80000000"
        app:layout_constraintStart_toStartOf="@+id/mrz_guide_frame"
        app:layout_constraintEnd_toEndOf="@+id/mrz_guide_frame"
        app:layout_constraintTop_toBottomOf="@+id/mrz_guide_frame"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <View
        android:id="@+id/mrz_guide_frame"
        android:layout_width="0dp"
        android:layout_height="60dp"
        android:background="@drawable/mrz_frame"
        app:layout_constraintVertical_bias="0.75"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintWidth_percent="0.9" />

    <!-- 3. Status Text / Kết quả: Hiển thị trạng thái quét -->
    <TextView
        android:id="@+id/statusText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:gravity="center"
        android:padding="10dp"
        android:text="Đặt MRZ vào khung vàng, giữ thẳng và đủ sáng"
        android:textColor="@android:color/white"
        android:textSize="16sp"
        android:textStyle="bold"
        android:background="#99000000"
        app:layout_constraintTop_toBottomOf="@id/mrz_guide_frame"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintWidth_percent="0.9"/>

    <!-- 3b. OCR Debug Panel: Đưa lên đầu màn hình để dễ theo dõi -->
    <TextView
        android:id="@+id/ocrDebugText"
        android:layout_width="0dp"
        android:layout_height="120dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="8dp"
        android:background="#4D000000"
        android:textColor="@android:color/white"
        android:textSize="12sp"
        android:padding="8dp"
        android:scrollbars="vertical"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/torchToggle"/>

    <!-- 4. Torch toggle: simple square tap target in top-right -->
    <View
        android:id="@+id/torchToggle"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:background="#66FFFFFF"
        android:contentDescription="Bật/tắt đèn flash"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="16dp"
        tools:visibility="visible"/>

    <!-- 5. Guidance hint: distance/angle/lighting -->
    <TextView
        android:id="@+id/guidanceText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:gravity="center"
        android:padding="8dp"
        android:text="Gợi ý: Giữ thẳng, đủ sáng, cách ~20–30cm"
        android:textColor="#B3FFFFFF"
        android:textSize="14sp"
        android:background="#33000000"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintWidth_percent="0.9"/>

</androidx.constraintlayout.widget.ConstraintLayout>
